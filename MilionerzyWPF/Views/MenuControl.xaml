<UserControl x:Class="MilionerzyWPF.MenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MilionerzyWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Nowa gra"   Command="{Binding CmdNewGame}"/>
        <Button Grid.Row="1" Content="Ustawienia" Command="{Binding CmdSettings}"/>
        <Button Grid.Row="2" Content="Wyniki"     Command="{Binding CmdScores}"/>

        <Button Grid.Row="4" Content="Zakończ" Click="Exit_Click"/>
    </Grid>
</UserControl>
